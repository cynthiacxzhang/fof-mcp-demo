# Flow-of-Funds MCP Demo — environment variables
# Copy to .env and fill in your values: cp .env.template .env

# Required: Anthropic API key for the agentic loop in api.py
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: port for the FastAPI server (default: 8000)
PORT=8000

# --- Future Trino / DuckDB connection (not yet wired up) ---
# Uncomment and populate when replacing the in-memory DuckDB with a real data lake

# TRINO_HOST=trino.example.com
# TRINO_PORT=443
# TRINO_USER=your_username
# TRINO_CATALOG=hive
# TRINO_SCHEMA=banking
# TRINO_HTTP_SCHEME=https
# TRINO_AUTH_TOKEN=your_token_here

# DUCKDB_PATH=/data/banking.duckdb
